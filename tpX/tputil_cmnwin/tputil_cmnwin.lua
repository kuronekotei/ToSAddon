--[[
	日本語
	関数群を保存している
	ゲーム機能を呼び出す　UI部は(なるべく)持たない関数はここに置く
--]]
local g0 = GetTpUtil();
local thisUtilVer = 1;
if(thisUtilVer < g0.UtilVer) then
	return;
end
g0.UtilVer = thisUtilVer;


function TPUTIL_CMNWIN_ON_INIT(adn, frame)
end
